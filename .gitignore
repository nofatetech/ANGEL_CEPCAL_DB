# Raw congress source media — kept local, not in the DB repo (large binaries).
# Transcripts under Videos_CEPCAL_2026/transcript/ ARE tracked (they are the data).
*.zip
*.mp4
Videos_CEPCAL_2026/*.mp4

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.obsidian/
